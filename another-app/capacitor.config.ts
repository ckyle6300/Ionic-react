import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chrisk.memories',
  appName: 'Memories',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
