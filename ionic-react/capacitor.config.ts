import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.chris.starter.first.ionic.app',
  appName: 'BMI Calculator',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
