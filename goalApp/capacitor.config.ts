import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.goalAppChris',
  appName: 'goalApp',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
